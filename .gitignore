*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Subdirectory builds (multi-module)
**/build/

# Keystores — NUNCA al repo
*.jks
*.keystore

# Release artifacts
app/release/

# Firebase config — contiene API key de cliente (restringida por SHA/package)
# Descomenta si quieres excluirla del repo y distribuirla por otro canal (CI secret)
# app/google-services.json
